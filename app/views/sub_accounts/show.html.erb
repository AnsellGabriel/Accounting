

<div class="card">
  
  <div class="card-header">
    <h3>Payee details</h3>
    <%= button_to "Delete", @sub_account, method: :delete, class: 'btn btn-danger' %>
  </div>
  <div class="card-body">
    
    <%= render @sub_account %>
  </div>
  <div class="card-footer">
    <%= link_to "Edit", edit_sub_account_path(@sub_account), class: "btn btn-secondary btn-sm" %> |
  <%= link_to "Back", sub_accounts_path, class: "btn btn-secondary btn-sm" %>
  </div>

</div>